# Scanner Models

Put the mobile ONNX image embedding model here, for example:

```text
assets/models/card-visual-reranker.onnx
```

The model output must match the embeddings stored in Supabase `card_clip_embeddings` for the same `model` value.
